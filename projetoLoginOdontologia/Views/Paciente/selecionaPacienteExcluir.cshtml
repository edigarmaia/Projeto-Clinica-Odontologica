@model projetoLoginOdontologia.Models.modelPaciente
@{
    ViewBag.Title = "selecionaPacienteExcluir";
}

<h2>Exclusão do Paciente</h2>

@Html.Raw(ViewBag.GridViewString)


@using (Html.BeginForm("excluirPaciente", "Paciente", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <label>Paciente</label>
    <br />
    <div class="editor-label">
        Digite o código do paciente a ser excluído:
    </div>
    <div class="editor-field">
        @Html.EditorFor(m => m.CodPac)
    </div>

    <br />
    <input type="submit" value="Selecionar" />
}


